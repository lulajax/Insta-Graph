## 📄 系统文档：Insta-Graph 社群分析器

### 1\. 系统总览 (System Overview)

#### 1.1. 系统目标

本系统旨在通过采集 Instagram 博主的**社交关系**（关注）和**内容关联**（共同被标记），将其存入图数据库。系统利用图查询算法，帮助分析师从一批“种子博主”出发，高效地拓展和发现**共性高**的“新博主”。

#### 1.2. 核心思想

  * **价值核心：** 系统的核心价值不在于数据量，而在于对“共同被标记” (`[:CO_TAGGED]`) 这种强关联关系的深度分析。

### 2\. 核心技术栈 (Tech Stack)

| 类别 | 技术选型 | 作用 |
| :--- | :--- | :--- |
| **图数据库** | **Neo4j** (v5.x) | 存储和查询节点（博主、帖子）与关系（关注、标记）。 |
| **后端 API** | **java21** / **Spring Boot** | 提供数据录入和分析查询的 HTTP 接口。 |
| **Java 驱动** | `neo4j` (Java Driver) | Spring Boot 与 Neo4j 之间的连接器。 |
| **数据录入前端**| **Vue** (推荐) | （可选）为人工录入提供一个简单的 Web 界面。 |
| **运行环境** | **Docker / Docker Compose** | 编排 Neo4j、Spring Boot 和 Vue 服务的部署。 |

-----

### 3\. 系统架构图 (Architecture)

```
+------------------+         +---------------------+         +-----------------+
|   人工操作员       |         |   [可选]              |         |  Spring Boot 后端   |
| (Analyst User)   |         |   Vue.js Frontend   |         |  (Java)             |
|                  |         |   (Data Entry UI)   |         |                     |
+------------------+         +---------------------+         +---------------------+
        |                            | (1) Calls API               |
        | (2) 直接使用 API (Swagger)  |---------------------------->| (3) 执行 Cypher 查询 |
        |-------------------------------------------------------->|                 |
        |                                                        +-----------------+
        |                                                                | (4)
        |                                                       +-----------------+
        |                                                       |   Neo4j 数据库  |
        |<------------------------------------------------------| (Graph Database)|
        | (5) 返回分析结果 (JSON)                                    +-----------------+
```

### 4\. 模块一：数据库设计 (Neo4j Schema)

这是系统的核心，严格按照“方法二”设计。

#### 4.1. 节点 (Nodes)

1.  **`:Blogger`** (博主节点)

      * **属性 (Properties):**
          * `username` (string, **主键**): 博主的 Instagram 用户名 (例如：`@username`)。
          * `seed_group` (string, **核心**): 标记该博主所属的“种子项目”。
              * **示例：** `busan_dancers` (釜山舞蹈项目)。
              * **逻辑：** 如果此值为 `null`，则代表该博主是“新发现的”或尚未分类。
          * `bio` (string, optional): 博主的个人简介。
          * `gender` (string, optional): 性别 (例如: `male`, `female`)。

2.  **`:Post`** (帖子节点)

      * **说明：** 重点用于处理“共同被标记”的关系。
      * **属性 (Properties):**
          * `post_id` (string, **主键**): 帖子的唯一 ID (例如 IG 的短链接 `CqXYZ...` 或 URL)。
          * `notes` (string, optional): 人工录入的备注，例如“XX 舞蹈室的视频”。
          * `latitude` (float, optional): 纬度。
          * `longitude` (float, optional): 经度。

3.  **`:Hashtag`** (标签节点)

      * **属性 (Properties):**
          * `name` (string, **主键**): 标签名称 (例如：`#부산댄스`)。

#### 4.2. 关系 (Edges / Relationships)

1.  **`[:FOLLOWS]`**

      * **方向：** `(:Blogger {username: 'A'})-[:FOLLOWS]->(:Blogger {username: 'B'})`
      * **含义：** 博主 A 关注了博主 B。
      * **录入：** 人工查看“关注中”列表录入。

2.  **`[:TAGGED_IN]`** (**最关键的关系**)

      * **方向：** `(:Post {post_id: 'P'})-[:TAGGED_IN]->(:Blogger {username: 'A'})`
      * **含义：** 帖子 P 标记了博主 A。
      * **录入：** 人工查看博主的“被标记的帖子”列表，找到一个帖子 P，然后看这个 P 标记了哪些人 (A, B, C...)。

3.  **`[:USES_HASHTAG]`**

      * **方向：** `(:Post {post_id: 'P'})-[:USES_HASHTAG]->(:Hashtag {name: 'H'})`
      * **含义：** 帖子 P 使用了标签 H。

-----

### 5\. 模块二：API 后端开发文档 (Spring Boot)

API 负责将人工录入的数据（“写”）和分析请求（“读”）转换为 Cypher 查询。

**根路径：** `/api/v1`

#### 5.1. 数据录入接口 (Write Endpoints)

这些接口用于人工录入数据，是系统的“写”操作。

**`POST /api/v1/blogger` (创建或更新博主)**

  * **说明：** 系统的入口。如果博主已存在，则更新；如果不存在，则创建。
  * **请求体 (Body):**
    ```json
    {
      "username": "dancer_A",
      "seed_group": "busan_dancers" // 如果是种子，则指定；如果是新发现的，可为 null
    }
    ```
  * **后端 Cypher (MERGE):**
    ```cypher
    MERGE (b:Blogger {username: $username})
    SET b.seed_group = $seed_group
    RETURN b
    ```

**`POST /api/v1/relationship/follow` (添加关注关系)**

  * **请求体 (Body):**
    ```json
    {
      "from_username": "dancer_A",
      "to_username": "dancer_B"
    }
    ```
  * **后端 Cypher (MERGE):**
    ```cypher
    // 确保两个博主节点都存在
    MERGE (a:Blogger {username: $from_username})
    MERGE (b:Blogger {username: $to_username})
    // 创建关注关系
    MERGE (a)-[r:FOLLOWS]->(b)
    RETURN r
    ```

**`POST /api/v1/relationship/co_tag` (添加共同标记 - 核心)**

  * **说明：** 这是最高效的录入接口。您提供一个帖子 ID 和所有被标记的人的列表。
  * **请求体 (Body):**
    ```json
    {
      "post_id": "CqXYZabc",
      "tagged_usernames": ["dancer_A", "dancer_B", "new_dancer_X"],
      "post_notes": "XX 舞蹈室 5 月考核视频"
    }
    ```
  * **后端 Cypher (Transaction):**
    ```java
    // (Spring Boot 端的伪代码逻辑)
    // 在一个事务 (transaction) 中执行以下操作:
    // 1. 创建或合并帖子节点
    tx.run("MERGE (p:Post {post_id: $post_id}) SET p.notes = $post_notes", 
        parameters("post_id", postId, "post_notes", postNotes));

    // 2. 循环处理所有被标记的博主
    for (String username : taggedUsernames) {
        // 2a. 确保博主存在（如果不存在，则创建，seed_group 为 null）
        tx.run("MERGE (b:Blogger {username: $username})", 
            parameters("username", username));
        // 2b. 创建帖子和博主的关系
        tx.run(
            "MATCH (p:Post {post_id: $post_id}) " +
            "MATCH (b:Blogger {username: $username}) " +
            "MERGE (p)-[r:TAGGED_IN]->(b)",
            parameters("post_id", postId, "username", username)
        );
    }
    ```

#### 5.2. 分析查询接口 (Read Endpoints)

这些接口用于获取分析结果。

**`GET /api/v1/analysis/common_follows` (共同关注分析)**

  * **说明：** 查找被指定 `seed_group` 中的博主共同关注最多的“新博主”。
  * **查询参数 (Query Params):**
      * `project`: string (例如：`busan_dancers`)
      * `min_follows`: int (可选, 默认=2): 至少被 N 个种子博主关注。
  * **后端 Cypher:**
    ```cypher
    // 1. 匹配指定项目 (project) 的所有种子博主
    MATCH (seed:Blogger {seed_group: $project})

    // 2. 找到他们关注的博主 (recommendation)
    MATCH (seed)-[:FOLLOWS]->(rec:Blogger)

    // 3. 关键：过滤掉已经是本项目种子的人
    WHERE rec.seed_group IS NULL OR rec.seed_group <> $project

    // 4. 统计每个推荐博主被种子博主关注的次数
    WITH rec, COUNT(seed) AS common_follow_count

    // 5. 过滤掉低于阈值的
    WHERE common_follow_count >= $min_follows

    RETURN rec.username, common_follow_count
    ORDER BY common_follow_count DESC
    LIMIT 100
    ```

**`GET /api/v1/analysis/co_tagged` (共同被标记分析 - 核心)**

  * **说明：** 查找与指定 `seed_group` 中的博主在**同一篇帖子**中被共同标记次数最多的“新博主”。
  * **查询参数 (Query Params):**
      * `project`: string (例如：`busan_dancers`)
      * `min_co_tags`: int (可选, B默认=2): 至少在 N 个不同的帖子里共同出现。
  * **后端 Cypher:**
    ```cypher
    // 1. 匹配指定项目 (project) 的所有种子博主
    MATCH (seed:Blogger {seed_group: $project})

    // 2. 找到他们被标记的帖子 (post)
    MATCH (seed)<-[:TAGGED_IN]-(post:Post)

    // 3. 找到这些帖子还标记了哪些其他人 (rec)
    MATCH (post)-[:TAGGED_IN]->(rec:Blogger)

    // 4. 关键：过滤掉已经是本项目种子的人
    WHERE rec.seed_group IS NULL OR rec.seed_group <> $project

    // 5. 统计每个推荐博主与种子博主共同出现的“帖子”数量
    WITH rec, COUNT(DISTINCT post) AS common_post_count

    // 6. 过滤掉低于阈值的
    WHERE common_post_count >= $min_co_tags

    RETURN rec.username, common_post_count
    ORDER BY common_post_count DESC
    LIMIT 100
    ```

-----

### 6\. 模块三：人工录入工作流 (Manual Entry Workflow)

**推荐工具：Vue 或 Spring Boot 自动生成的 /docs 界面**

您（操作员）的工作流程如下：

1.  **启动项目：** 确定一个项目名称，例如 `busan_dancers`。
2.  **录入种子：**
      * 打开 Instagram，找到您的 10 个种子博主。
      * 调用 `POST /api/v1/blogger` 10 次，将他们的 `username` 和 `seed_group: 'busan_dancers'` 录入。
3.  **录入“共同被标记” (核心)：**
      * **遍历**这 10 个种子博主。
      * 点击每个博主的\*\*“被标记的帖子”\*\*标签页。
      * 找到一个看起来是多人（例如舞蹈室视频）的帖子，记下它的 `post_id` (例如 `Cq...`)。
      * **点开这个帖子**，查看它标记了哪些人 (例如：`dancer_A`, `dancer_B`, `new_dancer_X`)。
      * 调用一次 `POST /api/v1/relationship/co_tag`，传入 `post_id` 和 `tagged_usernames` 列表。
      * （系统后端会自动创建 `new_dancer_X` 节点，且其 `seed_group` 为 `null`）。
      * **重复此操作**，录入 20-30 个高质量的“共同标记”帖子。
4.  **录入“关注” (可选)：**
      * （此步骤优先级低于 3）。
      * 查看种子博主 A 的“关注中”列表，如果发现他关注了 B（B 也是种子博主或您认识的圈内人），则调用 `POST /api/v1/relationship/follow`。
5.  **获取分析结果：**
      * 打开浏览器，访问 `GET /api/v1/analysis/co_tagged?project=busan_dancers`。
      * 系统将返回一个 JSON 列表，包含按共性排序的“新博主”，例如 `[{"username": "new_dancer_X", "common_post_count": 5}, ...]`。
      * 您现在可以去 Instagram 上审核 `new_dancer_X`，如果他符合要求，可以将其也添加为种子（调用 `POST /api/v1/blogger` 更新他的 `seed_group`），开始下一轮拓展。