# Insta-Graph 社群分析器操作说明

## 1. 系统概述

Insta-Graph 是一个专为 Instagram 社群分析设计的工具。它通过分析“共同标记”的关系，帮助运营人员高效地发现社群中的核心成员和潜在的合作伙伴。

## 2. 新人快速上手（推荐先看）

场景：你手里有一小批「已知的核心博主」，想快速找出和他们高度相关的新博主。

1. 打开系统首页，进入 **“工作流程”** 标签页。
2. 在 **第一步：选择或创建分组** 中，为当前项目创建一个新分组（如 `busan_dancers`），或选择已有分组。
3. 在 **第二步：批量添加种子博主** 中，将已知核心博主的用户名按行粘贴进去，点击 **批量添加种子**，系统会自动开始采集这些博主的数据。
4. 点击 **查看分析结果 →** 或切换到 **“分析结果”** 标签页，选择刚刚的分组，设置参数后点击 **🔍 开始智能分析**，即可看到系统推荐的新博主。
5. 在结果卡片中，将觉得合适的人 **晋升为种子博主**（进入下一轮分析），不合适的点 **放弃**。反复迭代，就能越挖越精准。

## 3. 主要功能模块

系统界面主要分为五个标签页，分别是：**工作流程**、**分组管理**、**分析结果**、**任务队列**和**数据管理**。

### 3.1. 工作流程（核心：开启和迭代分析）

作用：一步一步完成「建项目 → 加种子 → 看结果」的主流程。

操作分为三步：

1.  **第一步：选择或创建分组**
    *   **分组**是管理一批相关种子博主和分析结果的容器。
    *   您可以从“当前分组”下拉列表中选择一个已有的分组（下拉项会显示分组名及当前种子数量，如 `busan_dancers (12 个种子)`）。
    *   或者，在“创建新分组”输入框中输入一个有意义的名称（例如：`busan_dancers`），然后点击 **创建分组** 按钮来创建一个新的分析项目，系统会自动刷新分组列表。

2.  **第二步：批量添加种子博主**
    *   **种子博主**是您分析的起点，是您已知属于某个特定社群的核心成员。
    *   在文本框中，每行输入一个 Instagram 用户名（不需要加 `@`）。
    *   点击 **批量添加种子** 按钮。系统会自动将这些用户加入到当前分组，并开始在后台采集他们的公开数据（如关注列表、帖子、被标记的帖子等），上方徽章会实时显示该分组的种子数量。
    *   当前分组的种子详情以摘要卡片形式展示（例如“该分组已有 X 个种子博主”），不再逐条列出所有用户名。

3.  **第三步：查看分析结果**
    *   完成种子博主添加后，您可以点击 **前往分析结果页 →** 按钮，系统将自动切换到“分析结果”标签页，以便您进行下一步的分析。

### 3.2. 分组管理（管理不同项目）

作用：集中查看你名下的所有「项目分组」，方便切换和管理。

*   这个页面以卡片形式展示了您创建的所有分组，包括分组名称和当前种子数量（例如：`🌱 15 个种子`）。
*   您可以点击分组卡片快速切换到“工作流程”，继续为该分组添加种子。
*   点击卡片上的 **查看分析** 按钮可以直接跳转到“分析结果”标签页查看该分组的分析结果。
*   对于尚未包含任何种子的空分组，会显示 **删除** 按钮，可直接删除该分组。

### 3.3. 分析结果（找到「像他们」的新博主）

作用：基于种子博主的「共同标记关系」，推荐最值得关注的新博主。

1.  **选择分组**：首先，从下拉列表中选择您想要分析的分组。
2.  **设置分析参数**：
    *   **最小共同标记次数**：设置一个博主至少要在多少个不同的帖子中与种子博主一起被标记。建议新人从 `1` 或 `2` 开始尝试。
    *   **最小种子覆盖人数**：设置一个博主至少要和多少个不同的种子博主有连接（例如共同被标记）。建议从 `2` 开始，根据结果再慢慢调高。
3.  **开始分析**：点击 **🔍 开始智能分析** 按钮。
4.  **查看和导出结果**：
    *   系统会根据「覆盖多少个种子」和「共同被标记多少次」自动打分，分数越高越值得关注，不需要记公式，只要看排序即可。
    *   分析结果以卡片形式展示：显示推荐博主的排名、用户名、综合评分，以及覆盖人数与共同标记两部分得分的进度条。
    *   在结果卡片中，可点击“连接的种子数”查看与该博主有关联的种子列表，也可查看具体的共同标记帖子列表（含直达 Instagram 帖子链接），帮助你做人工判断。
    *   您可以直接在结果卡片中将博主 **晋升为种子博主**（进入下一轮分析）或 **放弃**（不再重点关注）；这些操作会实时影响后续分析结果。
    *   您可以点击 **📥 导出结果** 按钮，将当前分析结果导出为 CSV 文件，方便后续处理。

### 3.4. 任务队列（查看采集进度）

作用：查看和管理「采集数据」的执行情况，避免频繁手动刷新。

*   **批量采集**：在顶部输入框中，每行填写一个 Instagram 用户名，点击 **批量加入队列**，系统会依次为这些用户采集数据。这适合提前铺底一批潜在博主。
*   **队列状态**：
    *   **当前运行**：显示正在进行数据采集的任务（包括开始时间和加载动画）。系统同一时间只会处理一个任务。
    *   **待执行队列**：显示等待处理的任务列表，并支持对单条任务执行 **取消** 操作。
    *   **最近完成**：显示已经完成的数据采集任务，包括成功或失败状态；对于失败的任务，您可以直接点击 **重新采集** 重新加入队列。
    *   您可以随时点击 **🔄 刷新** 按钮或切换回该标签页来更新队列状态，顶部标签右侧的角标会显示当前活跃任务数量。

### 3.5. 数据管理（清理和校正博主信息）

作用：批量清理不相关博主、修正分组归属、查看单个博主详情。

*   **批量操作**：
    *   **批量放弃博主**：您可以将在文本框中输入一批您认为不相关的博主用户名（每行一个），可以在用户名后附加放弃原因，然后点击 **批量放弃**。被放弃的博主在后续的分析中会被弱化或过滤掉。
*   **数据筛选与浏览**：
    *   您可以通过 **搜索用户名**、**筛选分组** 或 **筛选状态**（活跃/已放弃）来快速查找特定的博主。
    *   数据会以表格形式展示，包含博主用户名、全名、简介、所属种子组、状态、Instagram ID 以及操作列。
    *   在操作列中，可以对单个博主执行 **种子连接**（查看与哪些种子在同一帖子中出现）、**共同帖子**（查看相关的共同标记帖子）、**同步**（重新采集数据）、**放弃/恢复** 以及 **删除** 操作；同时可以在“种子组”下拉框中直接调整其所属分组。点击「种子连接/共同帖子」时，系统会弹出分组选择下拉框，并默认选中当前行的种子组，您也可以临时切换到其他分组再查看连接情况。
    *   您可以通过底部的分页控件浏览所有数据，并在右上角调整每页显示条数。
