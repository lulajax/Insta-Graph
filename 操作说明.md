# Insta-Graph 社群分析器操作说明

## 1. 系统概述

Insta-Graph 是一个专为 Instagram 社群分析设计的工具。它通过分析“共同标记”的关系，帮助运营人员高效地发现社群中的核心成员和潜在的合作伙伴。

## 2. 主要功能模块

系统界面主要分为五个标签页，分别是：**工作流程**、**分组管理**、**分析结果**、**任务队列**和**数据管理**。

### 2.1. 工作流程 (核心)

这是开始一个新分析任务的标准流程，分为三个步骤：

1.  **第一步：选择或创建分组**
    *   **分组**是管理一批相关种子博主和分析结果的容器。
    *   您可以从“当前分组”下拉列表中选择一个已有的分组。
    *   或者，在“创建新分组”输入框中输入一个有意义的名称（例如：`busan_dancers`），然后点击 **创建分组** 按钮来创建一个新的分析项目。

2.  **第二步：批量添加种子博主**
    *   **种子博主**是您分析的起点，是您已知属于某个特定社群的核心成员。
    *   在文本框中，每行输入一个 Instagram 用户名（不需要加 `@`）。
    *   点击 **批量添加种子** 按钮。系统会自动将这些用户加入到当前分组，并开始在后台采集他们的公开数据（如关注列表、帖子、被标记的帖子等）。

3.  **第三步：查看分析结果**
    *   完成种子博主添加后，您可以点击 **前往分析结果页 →** 按钮，系统将自动切换到“分析结果”标签页，以便您进行下一步的分析。

### 2.2. 分组管理

*   这个页面以卡片形式展示了您创建的所有分组。
*   您可以在这里快速浏览已有的项目，并了解每个分组下的种子博主数量等基本信息。
*   点击 **+ 新建分组** 按钮可以快速切换到“工作流程”标签页来创建新分组。

### 2.3. 分析结果

这是进行核心社群发现功能的地方。

1.  **选择分组**：首先，从下拉列表中选择您想要分析的分组。
2.  **设置分析参数**：
    *   **最小共同标记次数**：设置一个博主至少要在多少个不同的帖子中与您的种子博主们一起被标记。这个值越高，代表该博主与您的种子社群关系越紧密。建议从 `1` 或 `2` 开始尝试。
    *   **最小种子覆盖人数**：设置一个博主至少与多少个不同的种子博主有连接（例如共同被标记）。这个值越高，代表该博主在社群中的覆盖面越广。
3.  **开始分析**：点击 **🔍 开始智能分析** 按钮。
4.  **查看和导出结果**：
    *   系统会根据“双维度评分机制”（综合考虑覆盖人数和共同标记次数）计算出得分，并按分数列出所有发现的博主。
    *   分析结果会以卡片形式展示，包含博主的用户名、得分、以及与哪些种子博主有关联。
    *   您可以点击 **📥 导出结果** 按钮，将分析出的博主列表导出为 CSV 文件，方便后续处理。

### 2.4. 任务队列

由于数据采集需要时间且为了避免 Instagram 的 API 限制，所有的数据采集任务都在一个队列中依次执行。

*   **批量采集**：您可以在此页面批量添加需要采集数据的 Instagram 用户名，将他们加入到全局任务队列中。这适用于您想要预先采集某些用户信息，但不把他们作为特定分析项目的“种子”的场景。
*   **队列状态**：
    *   **当前运行**：显示正在进行数据采集的任务。系统同一时间只会处理一个任务。
    *   **待执行队列**：显示等待处理的任务列表。
    *   **最近完成**：显示已经成功完成数据采集的任务。
    *   您可以随时点击 **🔄 刷新** 按钮来更新队列状态。

### 2.5. 数据管理

这个模块提供了对数据库中所有博主数据的全局管理功能。

*   **批量操作**：
    *   **批量放弃博主**：您可以将在文本框中输入一批您认为不相关的博主用户名（每行一个），并可以附上原因，然后点击 **批量放弃**。被放弃的博主在后续的分析中可能会被过滤掉。
*   **数据筛选与浏览**：
    *   您可以通过 **搜索用户名**、**筛选分组** 或 **筛选状态**（活跃/已放弃）来快速查找特定的博主。
    *   数据会以表格形式展示，包含了博主的用户名、全名、简介、所属种子组等信息。
    *   您可以通过底部的分页控件来浏览所有数据。
